# 🛠 Developer Guide – FLAC Media Converter

## 📌 專案簡介
FLAC Media Converter 是一個跨平台的音訊轉換工具，基於 **Qt 6 + ffmpeg**，支援多語系、Profile 管理、批次轉換與進度追蹤。本手冊提供開發者快速上手的指南，涵蓋環境建置、開發流程、版本管理與協作規範。

---

## ⚙️ 開發環境需求
* **C++17 或以上**
* **Qt 6.6+**
* **CMake 3.20+**
* **ffmpeg / ffprobe**
* **Git**

---

## 🚀 專案結構
```
src/        # 核心程式碼
profiles/   # 內建 Profiles JSON
i18n/       # 語系檔 (.ts/.qm)
config/     # 預設設定檔
tests/      # 測試程式
packaging/  # 打包腳本
docs/       # 文件與設計
```

---

## 🌱 開發流程
1. Fork 專案並建立分支：
```
git checkout -b feature/your-feature
```
2. 開發與測試：
```
cmake -S . -B build
cmake --build build
ctest --test-dir build
```
3. 提交修改：
```
git commit -m "feat(converter): 新增進度解析"
```
4. Push 並發送 Pull Request。

---

## 📝 Commit 規範（Conventional Commits）
* **feat**: 新功能
* **fix**: 錯誤修正
* **docs**: 文件更新
* **style**: 程式碼風格（不影響功能）
* **refactor**: 重構
* **perf**: 效能優化
* **test**: 測試
* **chore**: 雜項（CI/CD、建置流程）

範例：
```
feat(profile): 新增 Profile 匯入/匯出功能
fix(ui): 修正進度對話框在 macOS 上顯示錯誤
```


---

## 🐛 Issue 與 PR 流程
* **Issue**：請使用標籤分類（```bug```, ```feature```, ```docs```, ```enhancement```）。
* **PR**：需通過 CI/CD 測試，並更新文件（若有需要）。

---

## 🔄 版本管理策略
* 採用 **Semantic Versioning**：```MAJOR.MINOR.PATCH```
* 每次發佈版本：
	1. 更新 ```CHANGELOG.md```
	2. 修改 ```CMakeLists.txt``` 版本號
	3. 建立 Git Tag：```git tag v0.3.0 && git push origin v0.3.0```
	4. GitHub Actions 自動打包並建立 Release

---

## 🤖 CI/CD 自動化
* **Build & Test**：每次 push/PR 自動編譯並執行測試
* **Release**：建立 Tag → 自動打包 Windows/macOS/Linux 版本並附加到 Release
* **Changelog**：依照 commit message 與 Issue 標籤自動生成

---

## 📜 文件與翻譯
* 所有文件放在 ```docs/```
* 翻譯檔放在 ```i18n/```
* 新增語言時需更新：
	1. ```.ts``` 翻譯檔
	2. [README.md](README.md) 語言支援清單
	3. 設定對話框語言選項

---

## ✅ 開發者檢查清單
- [ ] 程式碼可編譯並通過測試
- [ ] Commit 訊息符合規範
- [ ] 文件已更新（README/CHANGELOG/Docs）
- [ ] PR 已關聯對應 Issue（若有）